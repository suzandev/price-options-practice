import PropTypes from "prop-types";

const PriceOption = ({ option }) => {
  return (
    <div>
      <h3>this is price option</h3>
    </div>
  );
};

PriceOption.propTypes = {
  option: PropTypes.node,
};

export default PriceOption;
